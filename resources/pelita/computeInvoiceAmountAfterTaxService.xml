<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="computeInvoiceAmountAfterTaxService">

    <select id="getInvoiceAmountAfterTaxById" resultType="hmap">
        SELECT
            id,
            coalesce(invoice_amount, 0.0) - coalesce(gst, 0.0) AS invoice_amount
        FROM invoices
        WHERE tenant_id = 59 AND invoice_no = '4356'
    </select>


    <update id="updateInvoiceAmountAfterTax">
        UPDATE invoices
        SET
            invoice_amount      = #{invoice_amount},
            invoice_outstanding = 0
        WHERE id = #{id}
    </update>
</mapper>