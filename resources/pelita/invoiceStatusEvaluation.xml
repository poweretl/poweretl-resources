<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="invoiceStatusEvaluation">

    <select id="getInvoiceStatusById" resultType="hmap">
        select id,case when invoice_outstanding &lt;= 0 then 12 else 6 end as invoice_status_id
        from invoices where tenant_id = #{tenant_id}
    </select>

    <update id = "updateInvoiceStatusEvaluation">
        update invoices set invoice_status_id = #{invoice_status_id}
        where id = #{id}
    </update>
</mapper>