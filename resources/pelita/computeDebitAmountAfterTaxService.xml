<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="computeDebitAmountAfterTaxService">

    <select id="getDebitAmountAfterTaxById" resultType="hmap">
        SELECT
        id,
        coalesce(debit_before_gst, 0.0) - coalesce(gst, 0.0) AS debit
        FROM transactions
        WHERE tenant_id = 59 AND gst > 0
    </select>


    <update id="updateDebitAmountAfterTax">
        UPDATE transactions
        SET
            debit      = #{debit}
        WHERE id = #{id}
    </update>
</mapper>